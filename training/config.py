CONFIG = {
    'dataset_path': 'train.csv',  # Path to train.csv
    'model_path': '../inference/model.joblib',  # Save to inference directory
    'vectorizer_path': '../inference/vectorizer.joblib'  # Save to inference directory
}